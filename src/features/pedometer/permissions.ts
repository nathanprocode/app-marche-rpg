import type { PedometerPermissionStatus } from "./types";

/**
 * Placeholder that will be connected to native APIs in the Expo app project.
 */
export async function getPedometerPermissionStatus(): Promise<PedometerPermissionStatus> {
  return "undetermined";
}
