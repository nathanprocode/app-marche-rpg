import { Text } from "react-native";
import { Screen } from "../src/ui/components/Screen";

export default function Onboarding() {
  return (
    <Screen>
      <Text style={{ color: "white" }}>Onboarding (Jour 2)</Text>
    </Screen>
  );
}
